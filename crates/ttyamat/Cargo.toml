[package]
name = "ttyamat"
version = "0.1.0"
edition = "2024"

[dependencies]
ttyamat-config = { version = "0.1.0", path = "../ttyamat-config" }
ttyamat-terminal = { version = "0.1.0", path = "../ttyamat-terminal" }
ttyamat-ui = { version = "0.1.0", path = "../ttyamat-ui" }
